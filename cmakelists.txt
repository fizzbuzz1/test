cmake_minimum_required(VERSION 3.10)
project(test VERSION 1.0)
find_package(SW REQUIRED)
sw_add_package(org.sw.demo.google.tesseract.tesseract
               org.sw.demo.danbloomberg.leptonica
               org.sw.demo.libarchive.libarchive)

sw_execute()

add_executable(out main.cpp image.cpp)
target_link_libraries(out PRIVATE ${SW_PACKAGES})